<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================
         WinmTech-Scada HandyControl 主题集成
         ======================================== -->

    <!-- 引用HandyControl默认主题 -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/skindefault.xaml" />
        <!-- <ResourceDictionary Source="WinmColors.xaml"/> -->
        <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
        <!-- 引用颜色定义 -->
        <ResourceDictionary Source="Colors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <!-- ========================================
         自定义HandyControl控件样式
         ======================================== -->

    <!-- 工业SCADA专用样式 -->
    <Style x:Key="WinmDeviceStatusPanel" TargetType="Border">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="#CED4DA" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="4" />
        <Setter Property="Padding" Value="15" />
        <Setter Property="Margin" Value="5" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="Gray"
                                  BlurRadius="8"
                                  ShadowDepth="2"
                                  Opacity="0.1" />
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 模块标题样式 -->
    <Style x:Key="WinmModuleTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- 状态指示器样式 -->
    <Style x:Key="WinmStatusIndicator" TargetType="Ellipse">
        <Setter Property="Width" Value="12" />
        <Setter Property="Height" Value="12" />
        <Setter Property="Stroke" Value="#CED4DA" />
        <Setter Property="StrokeThickness" Value="1" />
    </Style>

    <!-- 数值显示样式 -->
    <Style x:Key="WinmValueDisplay" TargetType="TextBlock">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="#007BFF" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!-- 单位显示样式 -->
    <Style x:Key="WinmUnitDisplay" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="#6C757D" />
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="Margin" Value="5,0,0,2" />
    </Style>

</ResourceDictionary>