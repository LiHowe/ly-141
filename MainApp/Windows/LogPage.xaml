<UserControl x:Class="MainApp.Windows.LogPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:windows="clr-namespace:MainApp.Windows"
             xmlns:models1="clr-namespace:Core.Models;assembly=Core"
             xmlns:viewModels="clr-namespace:MainApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:LogPageViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <!-- 转换器 -->
        <windows:LogLevelToStringConverter x:Key="LogLevelToStringConverter" />
        <windows:BooleanToYesNoConverter x:Key="BooleanToYesNoConverter" />
        <windows:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <windows:NullToBooleanConverter x:Key="NullToBooleanConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <!-- 日志条目样式 -->
        <Style x:Key="LogEntryListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="5,2" />
            <Setter Property="Margin" Value="0,1" />
        </Style>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="3" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,0,0,1"
                Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="系统日志" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" />
                    <TextBlock Text="{Binding LastUpdateTime, StringFormat='最后更新: {0}'}"
                               Margin="20,0,0,0" VerticalAlignment="Center" Foreground="#6C757D" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="清空当前日志" Margin="2"
                            Command="{Binding ClearLogFileCommand}"
                            IsEnabled="{Binding SelectedLogFile, Converter={StaticResource NullToBooleanConverter}}"
                            ToolTip="清空选中日志文件的内容" />
                    <Button Content="刷新" Width="60" Height="30" Margin="5,0"
                            Command="{Binding RefreshLogsCommand}" />
                    <Button Content="打开文件夹" Width="80" Height="30" Margin="5,0"
                            Command="{Binding OpenLogDirectoryCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- 左侧日志文件列表 -->
        <Grid Grid.Row="1" Grid.RowSpan="4" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- 文件列表标题 -->
            <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,0,0,1" Padding="10,8">
                <TextBlock Text="日志文件" FontWeight="Bold" FontSize="14" />
            </Border>

            <ListBox Grid.Row="1" ItemsSource="{Binding LogFiles}" SelectedItem="{Binding SelectedLogFile}">
                <ListBox.ItemTemplate>
                    <!-- <DataTemplate> -->
                    <!--     <TextBlock Text="{Binding FileName}"/> -->
                    <!-- </DataTemplate> -->
                    <DataTemplate>
                        <Grid Margin="5">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="{Binding FileName}" FontWeight="Bold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Row="1" Text="{Binding FormattedFileSize}"
                                       FontSize="11" />
                            <TextBlock Grid.Row="2" Text="{Binding FormattedLastModified}"
                                       FontSize="11" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- 文件列表 -->
            <!-- <ListView Grid.Row="1" ItemsSource="{Binding LogFiles}" -->
            <!--           SelectedItem="{Binding SelectedLogFile}" -->
            <!--           BorderThickness="0" -->
            <!--           ScrollViewer.HorizontalScrollBarVisibility="Disabled" -->
            <!--           ScrollViewer.VerticalScrollBarVisibility="Auto"> -->
            <!--     <ListView.ItemTemplate> -->
            <!--         <DataTemplate> -->
            <!--             <Grid Margin="5"> -->
            <!--                 <Grid.RowDefinitions> -->
            <!--                     <RowDefinition Height="Auto"/> -->
            <!--                     <RowDefinition Height="Auto"/> -->
            <!--                     <RowDefinition Height="Auto"/> -->
            <!--                 </Grid.RowDefinitions> -->
            <!-- -->
            <!--                 <TextBlock Grid.Row="0" Text="{Binding FileName}" FontWeight="Bold" -->
            <!--                           TextTrimming="CharacterEllipsis"/> -->
            <!--                 <TextBlock Grid.Row="1" Text="{Binding FormattedFileSize}" -->
            <!--                           FontSize="11" Foreground="#6C757D"/> -->
            <!--                 <TextBlock Grid.Row="2" Text="{Binding FormattedLastModified}" -->
            <!--                           FontSize="11" Foreground="#6C757D"/> -->
            <!--             </Grid> -->
            <!--         </DataTemplate> -->
            <!--     </ListView.ItemTemplate> -->
            <!-- </ListView> -->
        </Grid>

        <!-- 分隔线 -->
        <GridSplitter Grid.Row="1" Grid.RowSpan="4" Grid.Column="1"
                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                      Background="#DEE2E6" Width="1" />

        <!-- 过滤条件栏 -->
        <Border Grid.Row="1" Grid.Column="2" Background="White" BorderBrush="#DEE2E6" BorderThickness="0,0,0,1"
                Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="日志等级:" VerticalAlignment="Center" Margin="0,0,10,0" />
                <ComboBox Grid.Column="1" SelectedItem="{Binding SelectedLogLevel}" Margin="0,0,15,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource LogLevelToStringConverter}}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                    <ComboBox.Items>
                        <x:Null />
                        <models1:LogLevel>Debug</models1:LogLevel>
                        <models1:LogLevel>Info</models1:LogLevel>
                        <models1:LogLevel>Warning</models1:LogLevel>
                        <models1:LogLevel>Error</models1:LogLevel>
                    </ComboBox.Items>
                </ComboBox>

                <TextBlock Grid.Column="2" Text="搜索:" VerticalAlignment="Center" Margin="0,0,10,0" />
                <TextBox Grid.Column="3" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Margin="0,0,15,0" VerticalAlignment="Center" />

                <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="0,0,15,0">
                    <CheckBox Content="自动刷新" IsChecked="{Binding IsAutoRefreshEnabled}"
                              VerticalAlignment="Center" Margin="0,0,10,0" />
                    <TextBlock Text="间隔:" VerticalAlignment="Center" Margin="0,0,5,0" />
                    <TextBox Text="{Binding AutoRefreshInterval}" Width="40" VerticalAlignment="Center" />
                    <TextBlock Text="秒" VerticalAlignment="Center" Margin="5,0,0,0" />
                </StackPanel>

                <Button Grid.Column="6" Content="重置过滤" Width="70" Height="25"
                        Command="{Binding ResetFilterCommand}" />
            </Grid>
        </Border>

        <!-- 日志列表 -->
        <Grid Grid.Row="2" Grid.Column="2">
            <ListView ItemsSource="{Binding FilteredLogEntries}"
                      SelectedItem="{Binding SelectedLogEntry}"
                      ItemContainerStyle="{StaticResource LogEntryListViewItemStyle}"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="时间" Width="150">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FormattedTimestamp}" FontFamily="Consolas" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="等级" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding LevelColor}" CornerRadius="3" Padding="5,2">
                                        <TextBlock Text="{Binding LevelText}" Foreground="White"
                                                   FontWeight="Bold" FontSize="10" HorizontalAlignment="Center" />
                                    </Border>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="线程" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Source}" FontFamily="Consolas" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="消息" Width="500">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Message}" TextWrapping="NoWrap"
                                               TextTrimming="CharacterEllipsis" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <GridViewColumn Header="异常" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        Text="{Binding HasException, Converter={StaticResource BooleanToYesNoConverter}}"
                                        HorizontalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- 加载指示器 -->
            <Border Background="#80FFFFFF"
                    Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,10" />
                    <TextBlock Text="正在加载日志..." HorizontalAlignment="Center" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="3" Grid.Column="2" Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0"
                Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusText}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="{Binding LogStatistics}" VerticalAlignment="Center"
                           FontWeight="Bold" Foreground="#495057" />
            </Grid>
        </Border>

        <!-- 详细信息面板 -->
        <Border Grid.Row="4" Grid.Column="2" Background="White" BorderBrush="#DEE2E6" BorderThickness="0,1,0,0">
            <Grid Margin="15">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="详细信息" FontWeight="Bold" Margin="0,0,0,10" />

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <StackPanel DataContext="{Binding SelectedLogEntry}">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="时间:" FontWeight="Bold" Margin="0,0,10,5" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FormattedTimestamp}"
                                       Margin="0,0,0,5" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="等级:" FontWeight="Bold" Margin="0,0,10,5" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding LevelText}" Margin="0,0,0,5" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="线程:" FontWeight="Bold" Margin="0,0,10,5" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Source}" Margin="0,0,0,5" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="消息:" FontWeight="Bold" Margin="0,0,10,5" />
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Message}" TextWrapping="Wrap"
                                       Margin="0,0,0,5" />

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="异常:" FontWeight="Bold" Margin="0,0,10,5"
                                       Visibility="{Binding HasException, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Exception, Mode=OneWay}"
                                     IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas"
                                     Background="#F8F9FA" BorderThickness="1" Padding="5"
                                     Visibility="{Binding HasException, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        </Grid>
                    </StackPanel>
                </ScrollViewer>

                <!-- 无选择提示 -->
                <TextBlock Grid.Row="1" Text="请选择一条日志记录查看详细信息"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Foreground="#6C757D" FontStyle="Italic"
                           Visibility="{Binding SelectedLogEntry, Converter={StaticResource NullToVisibilityConverter}}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>