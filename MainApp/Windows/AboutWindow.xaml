<Window x:Class="MainApp.Windows.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="关于 WinmTech-Scada" Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 头部信息 -->
        <Border Grid.Row="0" Padding="30">
            <Border.Background>
                <LinearGradientBrush StartPoint="0.2,0.2" EndPoint="1,1">
                    <GradientStop Color="{StaticResource PrimaryColor}" Offset="0" />
                    <GradientStop Color="{StaticResource PrimaryLightColor}" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel HorizontalAlignment="Center">
                <!-- 应用图标 -->

                <Image Source="/Resources/Images/logo_full.png" Width="70" Height="70" />

                <!-- 应用名称 -->
                <TextBlock x:Name="AppNameTextBlock" Text="WinmTech-Scada" FontSize="28" FontWeight="Bold"
                           Foreground="White" HorizontalAlignment="Center" />

                <!-- 副标题 -->
                <TextBlock x:Name="AppSubtitleTextBlock" Text="工业数据采集与追溯系统" FontSize="16"
                           Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,0" />
            </StackPanel>
        </Border>

        <!-- 主要内容 -->
        <hc:ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 版本信息 -->
                <Border Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="5" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="版本:" FontWeight="Bold" Margin="0,0,15,5" />
                        <TextBlock Grid.Row="0" Grid.Column="1" x:Name="AppVersionTextBlock" Text="v1.0.0"
                                   Margin="0,0,0,5" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="构建日期:" FontWeight="Bold" Margin="0,0,15,5" />
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="2024-01-21" Margin="0,0,0,5" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="数据库:" FontWeight="Bold" Margin="0,0,15,5" />
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="SQL Server" Margin="0,0,0,5" />
                    </Grid>
                </Border>

                <!-- 功能特性 -->
                <Border Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="5" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="主要功能" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="✓" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"
                                       Margin="0,0,10,0" />
                            <TextBlock Text="用户登录与权限管理" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="✓" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"
                                       Margin="0,0,10,0" />
                            <TextBlock Text="设备管理与状态监控" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="✓" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"
                                       Margin="0,0,10,0" />
                            <TextBlock Text="实时数据采集与注册" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="✓" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"
                                       Margin="0,0,10,0" />
                            <TextBlock Text="历史数据查询与导出" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="✓" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"
                                       Margin="0,0,10,0" />
                            <TextBlock Text="阈值管理与报警监控" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="✓" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"
                                       Margin="0,0,10,0" />
                            <TextBlock Text="系统更新与维护" />
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                            <TextBlock Text="✓" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"
                                       Margin="0,0,10,0" />
                            <TextBlock Text="模块化架构设计" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 技术特点 -->
                <Border Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="5" Padding="20" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="技术特点" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

                        <TextBlock Text="• 现代化的WPF界面设计，支持主题切换" Margin="0,0,0,5" />
                        <TextBlock Text="• 模块化架构，支持插件式扩展" Margin="0,0,0,5" />
                        <TextBlock Text="• 高性能的数据采集与处理引擎" Margin="0,0,0,5" />
                        <TextBlock Text="• 完善的日志记录与错误处理机制" Margin="0,0,0,5" />
                        <TextBlock Text="• 支持多种工业通信协议" Margin="0,0,0,5" />
                        <TextBlock Text="• 灵活的配置管理系统" Margin="0,0,0,5" />
                    </StackPanel>
                </Border>

                <!-- 版权信息 -->
                <Border Background="{DynamicResource SurfaceBrush}"
                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                        CornerRadius="5" Padding="20">
                    <StackPanel>
                        <TextBlock Text="版权信息" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />
                        <TextBlock Text="Copyright © 2024 WinmTech. All rights reserved." Margin="0,0,0,5" />
                        <TextBlock Text="本软件受版权法保护，未经授权不得复制或分发。" Margin="0,0,0,5" />
                        <TextBlock Text="技术支持: support@winmtech.com" Margin="0,0,0,5" />
                        <TextBlock Text="官方网站: www.winmtech.com" Margin="0,0,0,5" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </hc:ScrollViewer>

        <!-- 底部按钮 -->
        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SystemInfoButton" Content="系统信息" Width="100" Height="32" Margin="0,0,10,0"
                        Click="SystemInfoButton_Click" />
                <Button x:Name="CloseButton" Content="关闭" Width="80" Height="32"
                        Click="CloseButton_Click" Style="{DynamicResource ButtonPrimary}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>